import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    paddingVertical: 4,
    alignItems: 'center',
  },
  text: {
    paddingLeft: 10,
    fontWeight: 'bold',
  },
});

export default styles;
