import { StyleSheet } from 'react-native';

const CenterContainerStyle = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
});

export default CenterContainerStyle;
