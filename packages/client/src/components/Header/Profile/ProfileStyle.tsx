import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  leaveDoor: {
    paddingRight: 10,
  },
  level: {
    paddingRight: 5,
  },
});

export default styles;
